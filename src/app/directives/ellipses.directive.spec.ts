import { EllipsesDirective } from './ellipses.directive';

describe('EllipsesDirective', () => {
  it('should create an instance', () => {
    const directive = new EllipsesDirective();
    expect(directive).toBeTruthy();
  });
});
